query quizQuestions($subject: String!, $token: String){
  quizQuestions(subject:$subject, token: $token){
    id
    text
    code
    subject
    difficulty
    answers {
      text
    }
  }
}
